---
scope: Multi-agent LangGraph pipeline redesign for Mobile UI Assistant
purpose: Fix pipeline to match PRD JTBD: visible self-checking/recovery agents
altitude: feature (the generation pipeline within an existing solo/portfolio system)
updated: 2026-08-25T21:16
---

- (constraint) Brownfield code sweep: ui_generator.py graph is ui_generator->accessibility_reviewer->ui_reviewer->output (linear, no retry). Intent Parser/Layout Planner deliberately removed (comment at line 624) in favor of single LLM call - keep that decision, do not reintroduce
- (version) langgraph>=1.0.3 already in pyproject.toml, supports add_conditional_edges - no new dependency needed for retry routing
- (decision) AD-1 [ADOPTED]: Keep single-LLM-call generation (no reintroduced Intent Parser/Layout Planner nodes). Binds FR-1/FR-7. Prevents redundant preprocessing the codebase already found degrades output quality.
- (decision) AD-2: Validation becomes an in-graph node (validator) with conditional edge back to ui_generator on failure, max 2 retries, else forward to reviewers. Binds FR-1/FR-3/FR-8/NFR-reliability. Prevents validation-as-afterthought that current post-hoc call (outside graph) makes non-routable and invisible to self-correction JTBD.
- (decision) AD-3: Standardize a shared CheckResult{check, status: pass|fail|warn, message} shape returned by every check-producing node (lint, compilation, accessibility, design). Binds FR-2/FR-3. Prevents current unstructured prose lists (mixed 'Good:'/issue strings) that can't render generically as a verdict.
- (decision) AD-4: Every graph node appends a TraceStep{node, summary, detail} to state.trace list. Binds FR-2. Prevents pipeline reasoning being observable only via server stdout print(), unusable for a UI trace panel.
- (decision) AD-5: State adds retry_count:int and last_validation_errors:list[CheckResult], fed back into the regeneration prompt on retry. Binds AD-2. Prevents unterminated or memoryless retry loop that repeats the same mistake.
- (question) Deferred: real Kotlin/Gradle compilation (kotlinc invocation) vs current brace-counting heuristic in GradleMCP.check_compilation - out of scope for this pass, flag as known limitation in README
- (event) spine finalized (fast path, hobby/solo stakes - full reviewer gate skipped, user review requested instead)
